# CODEX Rules：测试标准

> 本文档定义所有测试相关的流程、标准和规范。

---

## 1. 测试执行顺序

每次代码修改后，默认按以下顺序执行：

1. `mypy src/` — 类型检查，必须首先通过
2. 受影响单元测试 — 按修改范围选择
3. 受影响集成测试 — 跨层改动时执行
4. 必要时再跑完整测试集

---

## 2. 分阶段测试标准

### 文档阶段（只有文档和骨架）

- 检查目录结构是否符合架构文档
- 检查必需文档是否存在
- 检查日志是否已追加

### 骨架阶段（有空壳代码）

- `mypy src/` 必须通过
- 入口文件与导入关系必须可解析
- 不允许出现循环 import

### 工具阶段（有真实工具实现）

- 先测单个工具
- 再测 EventBus 与 Executor 联动
- 再测最小端到端流程

### 模型接入阶段（DeepSeek / OpenAI SDK）

- 先测 provider 初始化
- 再测无 key 时的报错行为
- 再测单轮调用
- 最后测 tool calling 链路

---

## 3. 最小测试原则

- 小改动先跑最小相关测试
- 跨层改动再跑更大范围测试
- 不允许一上来就盲目全量测试
- 不允许未测试就声称完成

---

## 4. 测试失败处理标准

1. 先记录失败命令
2. 先记录失败输出摘要
3. 判断是修复还是回滚
4. 修复后必须重跑同一测试
5. 涉及跨层修改时，重跑至少一轮更高一级测试

---

## 5. 测试结果记录位置

每次代码测试后，必须至少更新：

- `docs/LOG.md` — 施工日志
- `docs/DEV_PROGRESS.md` — 总进度
- `docs/LAYER_STATUS.md` — 层状态

记录内容至少包括：
- 本次执行了什么测试
- 成功还是失败
- 如果失败，失败点是什么
- 是否已经修复并重测

---

## 6. 当前项目测试命令

| 命令 | 用途 |
|------|------|
| `mypy src/` | Python 类型检查 |
| `pytest` | 运行全部测试 |
| `pytest tests/ -v` | 详细模式运行测试 |
| `python -m src.main <任务>` | 冒烟测试（需要实现 main.py 后可用） |

---

## 7. 测试文件规范

- 测试文件放在 `tests/` 目录
- 命名格式：`test_<layer_or_feature>.py`
- 使用 `pytest` 框架
- 类型注解使用 Python typing 模块

---

## 8. 禁止事项

- 不允许编造测试结果
- 不允许未查看 `pyproject.toml` 就猜测试命令
- 不允许测试失败后不记录风险
- 不允许把"未测试"写成"已验证"
- 不允许把占位命令写成项目既定标准
